import Placeholder from "@/components/Placeholder";
export default function Page() {
  return <Placeholder icon="👥" title="User Management" desc="Invite, edit and remove dashboard users with role + scope — coming in the next build stage." />;
}
