import Placeholder from "@/components/Placeholder";
export default function Page() {
  return <Placeholder icon="📋" title="Price All User" desc="SuperAdmin view of all calculator rows across every user, with date and dealer filters — coming in the next build stage." />;
}
